- name: install and run nginx service
  hosts: web
  become: true
  tasks:
  - name: install nginx
    ansible.builtin.package:
     name: nginx
     state: present

  - name: run nginx service
    ansible.builtin.service:
     name: nginx
     state: started
     enabled: yes